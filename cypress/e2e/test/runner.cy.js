//const { CucumberRunner } = require('cypress-cucumber-preprocessor');

/*describe('Run Cucumber Tests Programmatically', () => {
  it('should run all feature files', () => {
    const runner = new CucumberRunner({
      // Define the path(s) to your feature files
      featurePaths: [
        'cypress/integration/features/feature1.feature',
        'cypress/integration/features/feature2.feature',
        'cypress/integration/features/feature3.feature'
      ],
      // Define any additional options you want to use for the tests
      // For example, environment variables
      env: {
        MY_VARIABLE: 'my value'
      }
    });

    // Run the tests and wait for them to finish
    runner.run().then((results) => {
      // Do something with the results
      console.log(results);
    });
  });
});*/
